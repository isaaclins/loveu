import '../styles/globals.css'; // Assuming you might want global styles later, create an empty one for now

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp; 
